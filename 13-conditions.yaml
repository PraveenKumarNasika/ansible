- name: check number of live sessions
  hosts: local
  connection: local
  vars:
    live_sessions: 6
  tasks: 
  - name: live sessions are more than 5
    debug: 
      msg: "Yes, Ansible took more than 5 sessions"
    when: live_sessions > 5