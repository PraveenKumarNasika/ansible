- name: install and start nginx
  hosts: frontend
  become: yes
  tasks:
  - name: install nginx
    package:
      name: nginx
      state: present
      
  - name: enable nginx
    service:
      name: nginx
      state: started
      enabled: yes
